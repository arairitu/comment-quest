# CLAUDE.md — コメントクエスト プロジェクト記憶

このファイルは Claude Code が自動で読み込むプロジェクト文脈です。

---

## プロジェクト概要

**コメントクエスト（Comment Quest）** — 視聴者参加型 TikTok LIVE RPG。
顔出しなしで自律稼働し、視聴者のコメントとギフトがリアルタイムでゲーム内行動に直結する。全員で1人の勇者を共有して育て、ダンジョンを潜り続ける。

**配信対象は日本だけでなくグローバル。** 2Dドット絵RPGは「視覚言語」で伝わるためグローバル向き（HPバー・ダメージ数字・スライム・ドラゴン・剣は言語を超えて通じる）。UIはアイコン/数字ファースト、コメントコマンドは言語非依存で設計する（後述「コマンド設計」を必読）。

詳細な企画は `docs/企画書.md` を必ず参照すること。

---

## 技術スタック

| 領域 | 技術 |
|---|---|
| ゲーム画面（フロント） | HTML Canvas + 素のJavaScript（フレームワーク不要） |
| 配信出力 | OBS の「ブラウザソース」で `game/index.html` を表示 |
| ギフト/コメント検知 | Python + `TikTokLive`（tiktok-live-connector）ライブラリ |
| フロント⇔サーバー通信 | WebSocket（Python → ブラウザへイベント送信） |
| ドット絵素材 | Aseprite で別途制作（最初は仮の四角でOK） |

---

## アーキテクチャ

```
TikTok LIVE
   │ コメント・ギフト
   ▼
[server/main.py]  Python — TikTokLiveでイベント受信
   │ WebSocket (JSON)
   ▼
[game/]  ブラウザ — Canvasでゲーム描画・状態管理
   │
   ▼
OBS ブラウザソース → TikTok配信
```

イベントは JSON で流す。例:
```json
{ "type": "comment", "user": "userA", "text": "⚔️" }
{ "type": "comment", "user": "userB", "text": "heal" }
{ "type": "gift",    "user": "userC", "giftName": "rose", "coin": 1, "count": 10 }
```

---

## コマンド設計（多言語対応 / グローバル対応の核）

**最重要。** 日本語キーワードだけだと海外勢が「見てるだけ」になる。コメント→アクション変換は **絵文字 ＋ 数字 ＋ 多言語キーワードの3併用**で受け付ける。

`events.js` に正規化テーブルを持ち、どの入力形式でも同じアクションIDに変換する。

| アクション | 絵文字 | 数字 | 多言語キーワード（部分一致・小文字化して判定） |
|---|---|---|---|
| 攻撃 (attack) | ⚔️ | 1 | 攻撃 / attack / atk / atacar / 공격 / 攻击 |
| 回復 (heal) | 💚 | 2 | 回復 / heal / hp / curar / 힐 / 治疗 |
| 守る (defend) | 🛡️ | 3 | 守る / defend / guard / def / 방어 / 防御 |
| 魔法 (magic) | 🔥 | 4 | 魔法 / magic / mag / magia / 마법 / 魔法 |
| 盗む (steal) | 💰 | 5 | 盗む / steal / gold / robar / 훔치기 / 偷 |

**実装方針:**
- コメント文字列を小文字化・トリムし、上記テーブルと照合してアクションIDを引く。
- マッチしないコメントは「応援」としてカウント（応援ゲージ上昇に寄与）。
- 画面の操作説明は **絵文字＋数字のみ**で表示すれば翻訳不要（例: `⚔️/1 ATK　💚/2 HEAL　🛡️/3 DEF`）。
- キーワードテーブルは1ファイルに集約し、後から言語を追加しやすくする。

**UI多言語化の原則:** テキストを減らしアイコン/数字/バーで伝える。これだけで大半が言語非依存になる。タイトルは "Comment Quest"、サブタイトルのみ多言語（例: "Grow the hero with comments & gifts!"）。

---

## 推奨ディレクトリ構成

```
comment-quest/
├── CLAUDE.md
├── README.md
├── docs/
│   └── 企画書.md
├── game/                 # ブラウザ側（OBSブラウザソース）
│   ├── index.html
│   ├── style.css
│   ├── main.js           # ゲームループ・全体統括
│   ├── state.js          # ゲーム状態（勇者/敵/ゲージ/ランキング）
│   ├── hero.js           # 勇者エンティティ
│   ├── enemy.js          # 敵・ボス
│   ├── events.js         # コメント/ギフト→アクション変換
│   ├── render.js         # Canvas描画
│   ├── ws.js             # WebSocket受信
│   └── assets/           # ドット絵スプライト
└── server/               # Python側
    ├── main.py           # TikTokLive + WebSocketサーバー
    ├── mock.py           # 開発用：偽コメント/ギフトを流すテストツール
    └── requirements.txt
```

---

## 🔰 今やること = Phase 1（MVP）

**いきなり全機能を作らない。** まず「コメントで敵を倒すライブRPG」として動くものを完成させる。

Phase 1 のスコープ:
- [ ] 2Dドット勇者が自動で敵と戦う（Canvasゲームループ）
- [ ] コメント（攻撃/回復/守る）に反応してアクション発生 ── **絵文字・数字・多言語キーワードの3併用で受付**（上記「コマンド設計」参照）
- [ ] ギフトで必殺技発動
- [ ] ボスHPバー / 勇者HPバー
- [ ] 応援ゲージ（コメント数で上昇）
- [ ] 貢献度ランキング表示
- [ ] ラストヒット表示（撃破した人の名前）
- [ ] リザルト画面（配信終了時）

**Phase 2 以降（今は作らない）:** ガチャ、装備育成、5ゾーン切替、週間イベント、月次シーズン。

---

## 開発の進め方ルール

1. **モックから始める。** TikTok本番接続の前に `server/mock.py` で偽イベントを WebSocket に流し、ゲーム側を完成させる。本番APIは最後に差し替える。
2. **OBS前提で作る。** 画面は縦型 9:16（1080×1920）想定。背景は透過不要、Canvasフルサイズ。
3. **状態は1箇所に集約。** ゲーム状態は `state.js` に集約し、描画とロジックを分離する。
4. **小さく動かして確認。** 1機能ごとに動作確認。大きな未テストコードを一気に書かない。
5. **コメントは日本語OK。** このプロジェクトは日本語コメント可。

---

## 注意点

- TikTok の利用規約・API取得方法は実装時に確認する（自動化配信のポリシーに注意）。
- **TikTok LIVE の収益化対象は国・地域で条件が異なり変更もあるため、配信者側の収益化可否を実装前に最新情報で確認する。**
- `TikTokLive` ライブラリは公式APIではなく非公式ラッパーなので、動作の安定性は要検証。
- **曜日イベントは時差を考慮する**（固定時刻だと地域差が出る）。配信セッション基準にするか、時間をローテーションさせて公平に。世界中に視聴者がいれば24時間配信の可能性も。
- 超大ギフトの効果は「勝ち確」にしない（攻撃力5倍＋被ダメ半減程度。企画書 第4章参照）。
- 高額ギフトだけが勝つゲームにしない設計を常に意識する（3層バランス）。
